# Repository Instructions

For work in this repository:

- All Codex commits and pull requests must target the `dev` branch in `CarapexDev/Carapex`.
- Do not open pull requests against `main` unless the user explicitly asks for it.
